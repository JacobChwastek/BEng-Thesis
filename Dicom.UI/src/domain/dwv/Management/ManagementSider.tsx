import * as React from "react";
import { Layout } from "antd";

import './ManagementSider.scss'

const { Sider } = Layout;

type Props = {

};

export const ManagementSider = (props: Props) => {
	return (
		<Sider width={500} collapsible className="management-sider">
			xd
		</Sider>
	);
};
